# Task 9 Report: Docker Compose + Nginx + tuning

## Status: DONE

## 1. File verification (verify-then-fix — no content changes needed)

All three pre-existing untracked files matched the spec verbatim; verified, not rewritten.

- `docker-compose.yml` (65 lines) — OK as-is:
  - postgres:16, mem_limit 1024m, `shared_buffers=256MB max_connections=100`, pg_isready healthcheck
  - redis:7, mem_limit 128m, `maxmemory 96mb allkeys-lru`, redis-cli healthcheck
  - api: build context `.` / dockerfile `api/Dockerfile`, mem_limit 100m, env PORT/DATABASE_URL/REDIS_ADDR/JWT_SECRET(${JWT_SECRET} from `.env`, 48 chars)/UPLOAD_DIR/SEED=true, uploads volume, depends_on postgres+redis healthy
  - nginx:alpine, mem_limit 32m, `8080:80`, mounts `./nginx/nginx.conf`, `./web`, `uploads`; volumes pgdata/redisdata/uploads
  - Note: volume names are `pgdata`/`redisdata` (not `postgres-data`/`redis-data` from plan text) — kept, since the controller-reviewed state accepted them and renaming buys nothing on fresh volumes.
- `api/Dockerfile` (12 lines) — OK as-is: `golang:1.26-alpine` build (matches `go 1.26.0` in go.mod; tag resolved fine), `COPY api/go.mod api/go.sum` + `go mod download`, `COPY api/`, `CGO_ENABLED=0 go build -o /atlas .`; runtime `alpine:3.21` + ca-certificates.
- `nginx/nginx.conf` (33 lines) — OK as-is: `upstream api:8080`, `/v1/` proxy with forwarded headers, `/uploads/` alias, `/` static root.
- `web/.gitkeep` — CREATED (new empty file). `web/` did not exist; the nginx bind mount `./web:/usr/share/nginx/html:ro` needs the host dir. No Task 10 content added.

## 2. Boot + migration + seed

- `docker compose up -d --build` — all images pulled, api built clean (22s go build), postgres+redis reached healthy.
- First api start failed seed as expected on fresh volumes: `seed: pq: relation "users" does not exist` (migration not yet applied). This also caused the first nginx start to exit: `host not found in upstream "api:8080"` (api container down at nginx start). Both transient ordering effects, not config defects.
- Migration applied ONCE via `psql` on fresh volumes (`migrations/001_init.sql`, 7x CREATE TABLE, not re-runnable — applied exactly once): `\dt` shows all 7 tables (users, classes, qr_sessions, attendance_records, absence_requests, notifications, audit_logs).
- Restarted api (`docker compose up -d api`) → auto-seed ran (`SEED=true`): `SELECT count(*) FROM users` = **5** (admin/teacher/class_rep/student/student — admin@atlas.local, teacher@atlas.local, rep@atlas.local, s1/s2@atlas.local).
- Restarted nginx (`docker compose up -d nginx`) → Up, port 8080 bound.
- Proxy verified through nginx: `POST localhost:8080/v1/auth/login` with bad creds → HTTP 401 `{"error":"INVALID_CREDENTIALS"}` (response generated by the Go api, proving `/v1/` proxy path).

## 3. `docker compose ps -a` (final)

```text
NAME                    IMAGE            COMMAND                  SERVICE    STATUS                        PORTS
attendance-api-1        attendance-api   "/atlas"                 api        Up                            8080/tcp
attendance-nginx-1      nginx:alpine     "/docker-entrypoint.…"   nginx      Up                            0.0.0.0:8080->80/tcp
attendance-postgres-1   postgres:16      "docker-entrypoint.s…"   postgres   Up (healthy)                  5432/tcp
attendance-redis-1      redis:7          "docker-entrypoint.s…"   redis      Up (healthy)                  6379/tcp
```

postgres/redis report `healthy` (healthchecks defined); api/nginx report `Up` (no healthchecks in spec — expected).

## 4. Files changed / commit

Commit `350b2d3` — `feat(atlas): compose stack with memory budget` (scoped pathspecs only: `docker-compose.yml api/Dockerfile nginx/nginx.conf web/.gitkeep`):
- 4 files, 110 insertions(+), no modifications to tracked files.

## 5. Self-review

- Completeness: all three Task 9 steps done (files verified, boot+migrate+seed, commit). `ps` all up, migration single-applied, users = 5.
- Quality: zero content edits needed — files already matched spec; build is reproducible from clean state (fresh volumes used).
- Discipline/YAGNI: did not rename volumes to plan-text names, did not add healthchecks/restart policies/resolver hardening to nginx, did not build any Task 10 web content. Only addition is `web/.gitkeep` required for the bind mount.
- Test evidence: 401-through-nginx probe + users count query (above). No `go test` run (no Go code touched).

## 6. Issues / concerns

- `api` has no auto-migrate: any fresh clone/fresh-volume boot repeats the manual one-shot migration dance (api crash-loops until `001_init.sql` is applied). Acceptable per brief (single-apply by design), but Tasks 10–11 e2e docs should note the order: `up` → apply migration once → restart api.
- `nginx` resolves `api:8080` at startup: if nginx ever starts while no api container is running it exits(1) and needs a restart (observed once). Steady-state is fine; no file change made, but if e2e flakes on cold boot, consider `restart: unless-stopped` on nginx/api then.
- Migration is not re-runnable (no IF NOT EXISTS) — never re-apply blindly; check `\dt` first.
- Pre-existing untracked junk left alone per guards: `.superpowers/`, `android-mcp.log` (not committed).

---

## Fix report — round 1/5 (reviewer findings)

Base for diff: `350b2d3` (confirmed via `git rev-parse HEAD` before edit). Fix commit: `03364ab` — `fix(atlas): compose restart policy and fresh-boot order note` (scoped pathspec `docker-compose.yml` only, 5 insertions).

### What changed (`docker-compose.yml` only)
1. Finding 1 (Important — nginx `host not found in upstream api:8080` on first boot): added `restart: unless-stopped` to `api` and to `nginx`. No `depends_on` condition change, no new healthchecks (per scope: no healthchecks on api/nginx), volumes untouched (`pgdata`/`redisdata` stay).
2. Finding 2 (Important — no auto-migrate path, docs-only): added 3-line comment at top of `docker-compose.yml` noting fresh-boot order (`up → apply migrations/001_init.sql once → restart api`). No auto-migrate machinery added (out of scope/YAGNI, no code behavior change besides restart policy).

### Covering checks
- `docker compose config` — validates. Rendered config shows `restart: unless-stopped` on both `api` and `nginx`; `depends_on` intact (`api` → postgres/redis `service_healthy`; `nginx` → api `service_started`); volumes `pgdata`/`redisdata`/`uploads` unchanged.
- `docker compose up -d` — converges (api + nginx recreated for the new restart key, postgres/redis stayed healthy):
```text
Container attendance-postgres-1 Running
Container attendance-redis-1 Running
Container attendance-api-1 Recreate
Container attendance-api-1 Recreated
Container attendance-nginx-1 Recreate
Container attendance-nginx-1 Recreated
Container attendance-postgres-1 Waiting
Container attendance-redis-1 Waiting
Container attendance-postgres-1 Healthy
Container attendance-redis-1 Healthy
Container attendance-api-1 Starting
Container attendance-api-1 Started
Container attendance-nginx-1 Starting
Container attendance-nginx-1 Started
```
- `docker compose ps -a` (after fix):
```text
NAME                    IMAGE            COMMAND                  SERVICE    CREATED         STATUS                   PORTS
attendance-api-1        attendance-api   "/atlas"                 api        3 seconds ago   Up Less than a second    8080/tcp
attendance-nginx-1      nginx:alpine     "/docker-entrypoint.…"   nginx      2 seconds ago   Up Less than a second    0.0.0.0:8080->80/tcp, [::]:8080->80/tcp
attendance-postgres-1   postgres:16      "docker-entrypoint.s…"   postgres   4 minutes ago   Up 4 minutes (healthy)   5432/tcp
attendance-redis-1      redis:7          "docker-entrypoint.s…"   redis      4 minutes ago   Up 4 minutes (healthy)   6379/tcp
```

### Self-check vs scope
- Only `docker-compose.yml` touched (`git diff --stat`: 1 file, 5 insertions). No volume renames, no healthchecks added, no Task 10 web content touched.
